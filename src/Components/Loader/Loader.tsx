import { CircularProgress } from '@mui/material';
import { Box } from './styled';

const Loader = () => {
    return (
        <Box>
            <CircularProgress />
        </Box>
    );
};

export default Loader;
